import 'package:flutter/material.dart';

class IkeaProfilePage extends StatelessWidget {
  const IkeaProfilePage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(),
    );
  }
}
